# URL base da API (NestJS). Rodando a API localmente (fora do Docker), ela
# escuta em http://localhost:3000, então mantemos essa URL mesmo sem Docker.
VITE_API_URL=http://localhost:3000/v1

# Enquanto o módulo de autenticação (JWT) não estiver pronto no backend,
# mantenha "true" para logar com um usuário fake e navegar pelo layout.
VITE_AUTH_MOCK=true
